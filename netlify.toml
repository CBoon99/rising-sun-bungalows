# Static HTML — everything lives at repo root (index.html, assets/, thank-you.html).
# Netlify UI: Base directory = empty · Production branch = main
[build]
  command = "true"
  publish = "."

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
